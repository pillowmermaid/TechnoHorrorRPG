/*
A model representing each of the player's allies.
*/

define(
    [
        'backbone'
    ],
    function(Backbone){
        var PlayerUnit = Backbone.Model.extend();
        return PlayerUnit;
    }
);